---
title: Changing Node version if 'nvm not found'
date: 15.05.2023
description: If there is an 'nvm not found error', just follow this instructions.
category: node

---

If there is an 'nvm not found error', just follow this instructions.

```bash
➜  ~ nvm --version
zsh: command not found: nvm
```

```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
```

To open the bash file in vscode use:

```bash
code ~/.bashrc

```

To open the zsh file in vscode use:


```bash
code ~/.zshrc
```